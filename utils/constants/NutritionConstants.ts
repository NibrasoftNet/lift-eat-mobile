/**
 * FICHIER DE COMPATIBILITÉ UNIQUEMENT - NE PAS UTILISER DIRECTEMENT
 * 
 * Ce fichier est conservé uniquement pour maintenir la compatibilité avec le code existant.
 * Pour toutes les nouvelles importations, utilisez './nutrition-constants.ts' (kebab-case).
 *
 * Ce fichier sera supprimé dans une future version, après que toutes les dépendances 
 * auront été mises à jour pour utiliser le fichier kebab-case à la place.
 */

// Ré-exporte tout depuis le fichier kebab-case qui est maintenant la source unique de vérité
export * from './nutrition-constants';
