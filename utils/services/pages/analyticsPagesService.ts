/**
 * Service d'orchestration pour les pages d'analyse (UI)
 * Toute la logique métier doit passer par analyticsService (à créer si besoin).
 */
import { OperationResult } from "@/utils/interfaces/pages.interface";
// import { analyticsService } from "@/utils/services/analyticsService";

class AnalyticsPagesService {
  // Les méthodes d'orchestration seront ajoutées dès que la logique métier analytics sera définie
}

export const analyticsPagesService = new AnalyticsPagesService();
